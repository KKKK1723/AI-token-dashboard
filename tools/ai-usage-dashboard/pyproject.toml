[project]
name = "ai-token-dashboard"
version = "1.0.1"
description = "Generate the AI-token monitoring dashboard from CCSwitch statistics"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"
